import { SortByPointsAndNamePipe } from './sort-by-points-and-name.pipe';

describe('SortByPointsAndNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPointsAndNamePipe();
    expect(pipe).toBeTruthy();
  });
});
